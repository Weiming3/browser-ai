"""browser-ai CLI scripts package.

After `pip install browser-ai`, the entry point `browser-ai` is generated
from `scripts.browser_ai:main`. Local development can still run scripts
directly via `python scripts/browser_ai.py`.
"""

__version__ = "0.1.0"